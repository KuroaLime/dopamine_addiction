ta(FCurPlayerData OldCurPlayerData)\n{\n\tif (OldCurPlayerData.HoldingGold != CurPlayerData.HoldingGold)\n\t{\n\t\tOnGoldChnageNative.Broadcast(CurPlayerData.HoldingGold);\n\t}\n\tif (OldCurPlayerData.CurrentHP != CurPlayerData.CurrentHP)\n\t{\n\t\tOnHPChnageNative.Broadcast(CurPlayerData.CurrentHP);\n\t}\n}\n\t```\n\t이관 자체도 매끄럽지만은 않았다. 병합 충돌이 정리되지 않은 채 남아 `ResetState()`와 구매 처리 함수가 각각 두 번씩 정의되어 컴파일이 깨진 적이 있었고, 같은 시점에 캐릭터 액터 자체에 `SetReplicates(true)`가 빠져있던 것도 함께 발견해 고쳤다. 라운드 카운터도 같은 복제+델리게이트 패턴을 적용했는데, 이건 PlayerState가 아니라 매치 전체가 공유하는 `AMainGameState`에 뒀다 — 라운드는 개별 플레이어가 아니라 세션 전역의 값이기 때문이다. 여러 UI 클래스(`CPlayerLifeStateWidget`, `CRoundandTimerWidget` 등)의 델리게이트 바인딩 시그니처도 이 리팩토링에 맞춰 함께 수정했다.\n\tPlayerState로 옮기고 나서도 데이터 생명주기 문제가 완전히 끝난 건 아니었다. 플레이어가 접속을 끊었다 재접속하면 언리얼이 새 PlayerState를 만드는데, 이번엔 Pawn이 아니라 PlayerState 자체가 파괴·재생성되며 같은 종류의 데이터 유실이 재발했다. `AMainGameMode::Logout()`에서 현재 PlayerState의 데이터를 스냅샷으로 캡처해두고, `PostLogin()`에서 새로 생성된 PlayerState에 그 스냅샷을 복원하는 재접속 처리를 추가해 대응했다.\n\t## 배운 점\n\t- \"이 데이터의 생명주기가 무엇과 같이 가야 하는가\"를 먼저 따지지 않으면 나중에 훨씬 큰 리팩토링 비용이 든다는 것을 직접 경험\n\t- 언리얼의 Pawn vs PlayerState vs GameState 생명주기 차이를 실전에서 체득\n\t- 데이터를 올바른 객체로 옮겨도 그 객체 자체가 파괴·재생성될 수 있다는 것(재접속 시 PlayerState 재생성)까지 고려해야 생명주기 문제가 진짜로 끝난다는 것을 겪음\n</details>\n<details>\n<summary>05 - 체력 재생 컴포넌트</summary>\n\t## 문제/배경\n\t전투 중 데미지를 받다가 일정 시간 공격을 받지 않으면 서서히 체력이 회복되는 기능이 필요했다.\n\t## 설계 및 구현\n\t`HealthRegenComponent`를 `PossessedBy` 시점에 동적으로 생성/등록했다 — 폰 소유권이 실제로 설정된 시점에 붙여야 오너 참조가 유효함을 보장하기 때문이다. `OnTakeAnyDamage` 이벤트가 발생하면 재생 쿨다운 타이머를 리셋해 전투 중에는 회복이 시작되지 않도록 했다.\n\t회복량 계산은 처음부터 소수점 잔여값을 누적하는 방식으로 설계했다 — 매 틱 회복량을 정수로 즉시 반올림하면 깎이는 오차가 누적돼 실제 회복 속도가 설정값보다 느려지기 때문에, `FractionalHP`에 잔여값을 계속 더하다가 1.0을 넘는 순간에만 정수 HP로 반영했다.\n\t이 컴포넌트는 이후 다른 파일들과 병합하는 과정에서 실제 문제 두 가지를 드러냈다. 하나는 회복 상한이 하드코딩돼 있었다는 것이다 — `TickRegeneration`이 최대 체력을 구할 때 `PS->GetFinalMaxHP(100.0f)`처럼 기준값 100을 직접 박아 넣고 있었는데, 실제 캐릭터의 기준 최대 체력은 150이었다. 그 결과 자연 회복만으로는 최대 체력의 마지막 50을 영구히 채울 수 없는 상태였다. 다른 하나는 리슨 서버(호스트)가 자기 자신의 화면에서는 자연 회복이 UI에 반영되지 않는 문제였다 — 언리얼의 리플리케이션은 값을 변경한 서버 자신에게는 `OnRep_*`을 호출해주지 않기 때문에, 체력바 갱신이 `OnRep_CurPlayerData`에만 의존하는 구조에서는 서버 자신의 회복이 화면에 보이지 않았다. 두 문제 모두 여러 파일의 병합 충돌을 정리하던 시점에 함께 고쳤다 — 최대 체력 기준을 `AMainPlayerState::BaseMaxHealth`(150) 하나로 통일하고, HP가 실제로 바뀐 경우에만 명시적으로 델리게이트를 브로드캐스트하도록 만들었다:\n\t```c++\nvoid UHealthRegenComponent::TickRegeneration()\n{\n    APawn* PawnOwner = Cast<APawn>(GetOwner());\n    AMainPlayerState* PS = PawnOwner ? Cast<AMainPlayerState>(PawnOwner->GetPlayerState()) : nullptr;\n    if (PS)\n    {\n        float MaxHP = PS->GetCurrentMaxHP();\n        float RegenRate = PS->GetFinalRegenRate(0.0f);\n\n        float RecoveryAmount = RegenRate * RegenInterval;\n        FractionalHP += RecoveryAmount;\n        if (FractionalHP >= 1.0f)\n        {\n            int32 AddHP = FMath::FloorToInt(FractionalHP);\n            FractionalHP -= static_cast<float>(AddHP);\n            const int32 OldHP = PS->CurPlayerData.CurrentHP;\n            PS->CurPlayerData.CurrentHP = FMath::Min(static_cast<int32>(MaxHP), OldHP + AddHP);\n            if (PS->CurPlayerData.CurrentHP != OldHP)\n            {\n                PS->OnHPChnageNative.Broadcast(PS->CurPlayerData.CurrentHP);\n                PS->ForceNetUpdate();\n            }\n        }\n    }\n}\n\t```\n\t`GetCurrentMaxHP()`가 `BaseMaxHealth` 기준으로 계산되도록 고치면서 회복 상한 문제가 풀렸고, `OnHPChnageNative.Broadcast()`를 조건부로 직접 호출하면서 리슨 서버 자신의 화면도 다른 클라이언트와 동일하게 즉시 갱신되도록 만들었다.\n\t## 결과\n\t- 시간 기반으로 누적되는 값을 정수로 반영할 때는 매번 반올림하지 않고 잔여값을 누적해서 넘어갈 때만 반영해야 회복 속도 손실이 없다는 것을 설계 단계부터 고려함\n\t- 여러 곳에서 각자 계산하던 \"기준 최대체력\" 값을 하나로 통일하지 않으면, 리셋/회복/UI 비율 계산이 서로 다른 기준을 참조하며 조용히 어긋날 수 있다는 것을 병합 충돌을 고치며 체감\n\t- 언리얼 리플리케이션에서 서버 자신은 자기가 바꾼 값에 대해 `OnRep`이 호출되지 않는다는 함정을 이 컴포넌트에서 실제로 겪고, 값이 바뀌는 시점에 명시적으로 델리게이트를 쏴주는 패턴으로 대응하는 법을 익힘\n</details>\n<details>\n<summary>06 - 상점 시스템 (페이즈 제어 · UI · 무기 개조 경제)</summary>\n\t## 문제/배경\n\t라운드마다 전투(TPS) → 카드(섯다) → 상점이 순환하는 구조에서, 상점 페이즈 동안에는 이동/사격 등 전투 입력이 막혀야 했다. 또한 초기 상점 UI는 \"랜덤 카드 강화 1개 + 캐릭터 고정 스탯 강화 3개\" 버튼만 있어 화면이 비어 보였고, 무기 스탯(공격력/연사력/사거리/탄창/재장전)을 직접 구매로 올릴 수 있는 경로도 필요했다.\n\t## 설계 및 구현\n\t먼저 페이즈 제어부터 만들었다. `EDediServerPhase`에 `PreBattleShop`이라는 새 서버 페이즈를 도입하고, `bShopAvailable` 플래그를 리플리케이트해 서버(RPC 게이트)와 클라이언트(어빌리티 활성화 조건) 양쪽에서 동일한 조건으로 체크하도록 했다. 상점 페이즈 중에는 개별 어빌리티마다 조건문을 넣는 대신, `ShopInputHandler`로 입력 매핑 컨텍스트 자체를 나가기만 가능한 최소 입력으로 교체하는 방식을 택했다 — 이렇게 하면 새 어빌리티가 추가돼도 페이즈 제약을 따로 신경 쓸 필요가 없어진다. 처음 구현했을 때는 상점에 들어가면 마우스 커서만 보이게 하고 입력 모드는 `FInputModeGameOnly()`로 남겨뒀는데, 이 모드에서는 UI가 클릭을 받지 못하고 모든 입력이 그대로 캐릭터 이동으로 새어나갔다. 상점 진입 시에는 `FInputModeGameAndUI()`로 바꾸도록 고쳐서, 상점 UI가 입력을 제대로 받으면서 캐릭터 이동은 막히도록 정리했다.\n\t카드 버리기(`Ability_CardDiscard`)는 짧게 누르는 것과 길게 눌러 확정하는 것을 구분해야 했다. 홀드 확인 상태 머신(`StartHold/ConfirmHold/CancelHold`)을 만들어서 일정 시간 이상 누르고 있을 때만 확정되도록 처리했다:\n\t```c++\nvoid UAbility_CardDiscard::StartHold(AActor* InOwner, UWorld* World)\n{\n    if (!World) return;\n    bHoldConfirmed = false;\n    HoldOwner = InOwner;\n\n    World->GetTimerManager().SetTimer(\n        HoldConfirmTimerHandle, this, &UAbility_CardDiscard::ConfirmHold,\n        HoldThreshold, false);\n}\n\nvoid UAbility_CardDiscard::ConfirmHold()\n{\n    bHoldConfirmed = true;\n    if (AActor* Owner = HoldOwner.Get())\n    {\n        if (UTpsPlayerMainHUD* HUD = ResolveHUD(Owner))\n        {\n            HUD->ConfirmDiscardSelectedCard();\n        }\n    }\n    EndAbilityNow();\n}\n\t```\n\t상점의 랜덤 업그레이드 카드 3장 제시는 `Server_Request → Client_Receive → Server_Select` 형태의 RPC 왕복 구조로 구현했다. 상점 방벽은 처음엔 상점이 열리고 닫히는 두 시점에서만 콜리전을 켜고 끄는데, 그 사이에 있는 준비 라운드(`Ready`) 구간에는 방벽을 켜는 호출이 아예 없어서 상점이 열리기 전인데도 배틀로얄 구역으로 미리 걸어 들어갈 수 있는 틈이 있었다. 며칠 뒤(2026-07-22) 상점 방벽(`AA_Spawn`의 Barrier 태그 컴포넌트) 콜리전+가시성을 게임 페이즈 전환 지점(`MainGameMode`)에 연동해, 준비 라운드(`Ready`)와 상점 라운드(`PreBattleShop`)에서만 활성화되고 그 외 구간(전투/카드/결과)에서는 항상 비활성화되도록 네 지점 모두 정리했고, 같은 두 구간에서만 이동 입력도 잠그도록 `SetPlayerPawnGameplayState()` 호출 지점을 페이즈별로 재정리했다. 방벽 메시 자체도 원래는 콜리전 상태와 무관하게 항상 안 보이도록 만들어져 있어서 플레이어 입장에서는 이유 없이 막히는 투명 벽처럼 보였는데, 이때 콜리전 상태와 가시성을 동기화해 막혀 있을 때는 벽도 눈에 보이도록 함께 고쳤다.\n\t> 정직하게 짚을 점: 상점 방벽 콜리전이 켜지는 시점에 플레이어가 이미 그 자리에 텔레포트되어 있어 겹쳐서 \"바닥에 박히는\" 버그를 코드 추적으로 원인만 특정했고, 좌표 오프셋 보정 같은 근본 수정은 적용하지 않은 채 이동 잠금 해제로 임시 완화만 했습니다.\n\t같은 시점에 상점 UI와 경제 시스템도 확장했다. HUD(`TpsPlayerMainHUD`)가 쓰던 `Lv_Image[8]` + 다이나믹 머티리얼 인스턴스(`Linear_wipe` 스칼라 파라미터) 패턴을 상점 위젯에도 동일하게 적용해서, 체력/이동속도/체력재생 + 무기 스탯 5종의 현재 수치를 실시간 게이지로 보여주도록 이식했다. 이 과정에서 기존 HUD 코드(`UpdateLevel()`)에 존재 여부를 확인하는 배열과 실제로 역참조하는 배열이 다른 잠재 널 포인터 버그를 발견했다 — 지금까지는 이미지 위젯이 있으면 머티리얼도 항상 같이 있었기 때문에 우연히 문제가 없었을 뿐인데, 팀원의 블루프린트 리팩토링 커밋이 그 전제를 깨뜨리며 실제 크래시로 드러난 것이었다. git 커밋 diff까지 추적해 근본 원인을 특정하고 수정했다.\n\t상점 구매 RPC가 처음부터 이렇게 안전했던 건 아니었다. 원형이 된 캐릭터 고정 스탯 구매 RPC(`Server_SelectStaticUpgradeOption`)의 초기 구현은 골드 부족 체크가 통째로 주석 처리된 채 가격과 무관하게 고정 5골드만 차감했고, 페이즈 체크도 상점이 아니라 사실상 반대 의미인 배틀로얄 페이즈 여부를 검사하고 있었다:\n\t```c++\nif (CurrentPhase != EGamePhase::Shop || !GM || !GM->IsBattleRoyalePhase())\n    return;\n...\n/*int32 Cost = GetStaticUpgradeCost(UpgradeType, CurrentLevel);\nif (PS->CurPlayerData.HoldingGold < Cost) { return; }*/\nPS->AddGold(-5);\n\t```\n\t골드 체크를 되살리려던 다음 수정에서는 부호를 빠뜨려 오히려 구매할수록 골드가 늘어나는 버그를 만들었다가 같은 날 안에 다시 고쳤다. 이후 서버 RPC 전반에 `TryConsumeServerRpcRateLimit`을 도입하면서 이 함수에도 인덱스 범위를 검증하는 `_Validate`, 정확한 상점 페이즈 체크, 사망 중 구매 방지까지 한꺼번에 갖추게 되었다. 무기 개조 상점의 `Server_PurchaseWeaponUpgrade`는 이 패턴이 이미 정착된 뒤에 만들어졌기 때문에 처음부터 같은 보호 장치를 갖추고 시작할 수 있었다.\n\t무기 개조 상점은 상점 라운드(`PreBattleShop`)가 시작될 때마다 서버가 무기 스탯 5종 중 3종을 무작위로 뽑아 리플리케이트하는 방식으로 만들었다. 이 랜덤 상태를 어디에 저장할지가 고민이었는데, 상점 화면이 라운드 단위로 모든 플레이어가 같은 조건을 봐야 하는 구조라 `PlayerState`가 아니라 매치 전체가 공유하는 `AMainGameState`에 `ShopWeaponUpgradeOptions`로 리플리케이트하는 쪽을 택했다. 구매는 `Server_PurchaseWeaponUpgrade` RPC로 서버 권위 하에 페이즈/골드/레이트리밋을 모두 검증한 뒤에만 처리되도록 했다:\n\t```c++\nbool AMainPlayerController::Server_PurchaseWeaponUpgrade_Validate(int32 SlotIndex)\n{\n\treturn SlotIndex >= 0 && SlotIndex < 3;\n}\n\nvoid AMainPlayerController::Server_PurchaseWeaponUpgrade_Implementation(int32 SlotIndex)\n{\n\tif (!TryConsumeServerRpcRateLimit(\n\t\tShopWeaponUpgradeRateLimitState,\n\t\tShopUpgradeSelectionMinimumIntervalSeconds,\n\t\tTEXT(\"ShopWeaponUpgrade\")))\n\t{\n\t\treturn;\n\t}\n\n\tAMainGameMode* GM = GetWorld() ? GetWorld()->GetAuthGameMode<AMainGameMode>() : nullptr;\n\tAMainGameState* GS = GetWorld() ? GetWorld()->GetGameState<AMainGameState>() : nullptr;\n\tAMainPlayerState* PS = GetPlayerState<AMainPlayerState>();\n\tif (CurrentPhase != EGamePhase::Shop ||\n\t\t!GM ||\n\t\t!GM->IsShopRequestAllowed() ||\n\t\t!GS ||\n\t\t!PS ||\n\t\tPS->CurPlayerData.CurrentHP <= 0)\n\t{\n\t\treturn;\n\t}\n\t// ... 이후 골드 검증 및 구매 처리\n}\n\t```\n\t무기 개조는 캐릭터 고정 스탯 강화(100 Gold)보다 저렴한 고정가(70 Gold)로 책정하고 스탯 증가폭도 랜덤 카드 강화보다 작게(+0.5) 잡아, 카드 뽑기의 가치를 깎아먹지 않는 보조 구매 수단으로 자리잡도록 마무리했다.\n\t## 결과\n\t- 페이즈별 입력 제약을 각 어빌리티 내부 조건문으로 흩뿌리는 대신 입력 매핑 컨텍스트 교체로 처리하니, 새 어빌리티가 추가돼도 페이즈 제약을 따로 신경 쓸 필요가 없어짐\n\t- UMG `BindWidget` 프로퍼티는 디자이너의 위젯 이름과 C++ 프로퍼티 이름이 정확히 같아야 자동 바인딩되는데, 이름만 같고 `meta=(BindWidget)`을 빠뜨리면 \"이미 존재하는 프로퍼티\"라는 모호한 컴파일 에러가 난다는 걸 실전에서 겪고 원인을 특정함\n\t- 팀원의 리팩토링 커밋이 내 코드의 암묵적 전제를 깨뜨릴 수 있다는 것을 널 포인터 버그를 통해 실감함 — 내 코드의 정확성만큼이나 방어적 설계가 필요함을 체득\n\t- 서버 권위 랜덤 상태를 PlayerState가 아니라 GameState에 둘지 고민 후, 라운드 단위로 공유되는 화면 구조에 맞춰 GameState를 선택\n</details>\n<details>\n<summary>07 - 황금 고블린 AI (\"AI 디렉터\" 구현)</summary>\n\t## 문제/배경\n\t맵을 배회하다 처치하면 후한 보상을 주는 회피/도주형 레어 몬스터 \"황금 고블린\"을 만들면서, 몬스터를 고정된 위치나 완전히 무작위로만 스폰시키는 대신, 최근에 전투가 있었는지(소강 상태 여부)와 플레이어들의 위치를 계속 지켜보다가 \"적절한 순간\"에만 등장시키는 스폰 페이싱 시스템, 이른바 Left 4 Dead의 \"AI 디렉터\"를 이 프로젝트에 직접 구현해보는 것을 목표로 잡았다. 이 스폰 판단을 전담할 `UGoldenGoblinDirectorComponent`를 만들고, 디렉터가 실제로 등장시킬 대상으로 황금 고블린의 캐릭터(Character)/AI 컨트롤러/Behavior Tree도 함께 만들었다.\n\t## 설계 및 구현\n\t### 스폰 페이싱 — AI 디렉터 (이번 작업의 핵심 목표)\n\t개별 고블린의 행동(Behavior Tree)과 \"언제/어디에 등장시킬 것인가\"는 처음부터 완전히 분리해서 설계했다. `UGoldenGoblinDirectorComponent`는 GameMode가 소유하고 BattleRoyale 페이즈 동안에만 활성화되며, Character/AIController는 이 컴포넌트의 존재 자체를 모른다 — Character는 `TakeDamage`에서 `GetActive()`로 디렉터를 찾아 전투가 일어났다는 사실만 통지(`NotifyCombatEvent`)하고, 실제 스폰 판단은 전부 디렉터 쪽에 있다. 이 통지는 고블린 자신이 맞을 때뿐만 아니라 플레이어끼리의 TPS 교전에서 플레이어가 맞을 때도 함께 걸리도록 만들어서, \"소강 상태\" 판정이 고블린 관련 전투만이 아니라 맵 전체의 전투 상황을 반영하도록 했다:\n\t```mermaid\nflowchart LR\n    Player[플레이어 또는 고블린 피격] -->|NotifyCombatEvent| Director[UGoldenGoblinDirectorComponent]\n    Director -->|4가지 조건 충족 시| Spawn[AGoldenGoblinCharacter 스폰]\n    Spawn -->|TakeDamage로 사망| Delegate[OnGoblinDied 델리게이트]\n    Delegate --> Controller[AGoldenGoblinAIController]\n    Controller -->|Blackboard IsDead 갱신| BT[BTD_IsDead가 사망 브랜치 강제]\n\t```\n\t스폰 조건은 네 가지를 모두 만족해야 한다: 페이즈 시작 후 최소 대기시간, 최근 전투 이벤트가 일정 시간 이상 없는 \"소강 상태\", 모든 플레이어로부터 최소 거리 이상 떨어진 지점, 내비메시 상에서 실제로 도달 가능한 위치(재시도 횟수 제한). 이 중 최소 대기시간은 현재 테스트 편의상 10초로 낮추어둔 값이고, 실제 밸런싱 단계에서는 60초 이상으로 늘릴 계획이다.\n\t```c++\nif (Now - PhaseStartTimeSeconds < MinSpawnDelaySeconds) return;\nif (Now - LastCombatEventTimeSeconds < CombatLullSeconds) return; // 아직 소강 상태가 아님\n...\nif (FVector::DistSquared(CandidateLocation.Location, PlayerLoc) < MinDistanceSq) { ...; continue; }\n\t```\n\t첫 구현은 \"페이즈당 1회만 스폰\"하고 그 뒤로는 타이머 자체를 꺼버리는 방식(`bSpawnedThisPhase` 플래그)이었다. 이 방식은 스폰된 고블린이 일찍 죽어버리면 그 라운드 내내 다시는 등장하지 않는다는 한계가 있어서, 살아있는 개체를 약한 참조 배열(`TArray<TWeakObjectPtr<AGoldenGoblinCharacter>> ActiveGoblins`)로 계속 추적하고, 체크 주기마다 무효화/사망한 개체를 정리한 뒤 `MaxConcurrentGoblins`(2마리) 미만이면 계속 보충 스폰을 시도하는 방식으로 바꿨다 — \"한 번 등장시키고 끝\"이 아니라 \"동시에 몇 마리가 존재하는가\"를 디렉터가 지속적으로 관리하는 형태로 만든 것이다.\n\t```c++\nActiveGoblins.RemoveAll([](const TWeakObjectPtr<AGoldenGoblinCharacter>& Goblin)\n{\n    return !Goblin.IsValid() || Goblin->IsGoblinDead();\n});\nif (ActiveGoblins.Num() >= MaxConcurrentGoblins)\n{\n    return;\n}\n\t```\n\t이전에는 스폰에 성공하면 반복 타이머 자체를 멈춰버렸지만, 지금은 타이머를 계속 돌려서 개체 수가 줄어들 때마다 빈 자리를 자동으로 다시 채우는 상시 순환 스폰 루프로 바뀐 것이다.\n\t### 페이싱 대상 — 캐릭터 / AI 컨트롤러 / Behavior Tree\n\t디렉터가 실제로 등장시킬 대상으로 `AGoldenGoblinCharacter`(ACharacter)와 `AGoldenGoblinAIController`(AAIController)를 신규로 만들었다. 체력 시스템은 새로 만들지 않고 플레이어가 쓰는 것과 같은 자체 GAS(`UPFGASC`/`UPFGAttributeSet`)를 그대로 재사용해서, 데미지 적용·죽음 판정을 플레이어와 동일한 경로로 태웠다.\n\t```c++\nif (HasAuthority() && AbilitySystemComponent && AbilitySystemComponent->AttributeSet)\n{\n    UPFGAttributeSet* AttributeSet = AbilitySystemComponent->AttributeSet;\n    AttributeSet->SetMaxHealth(GoblinMaxHealth);\n    AttributeSet->ApplyHealthDelta(GoblinMaxHealth);\n}\n\t```\n\t`TakeDamage`는 `HasAuthority()`와 `bIsDead` 가드를 먼저 통과해야 데미지를 적용하고, 체력이 0 이하로 떨어지는 순간에만 `HandleDeath()`를 호출해 `OnGoblinDied` 델리게이트를 브로드캐스트한다. Character는 AIController를 전혀 몰라도 되고, AIController가 `OnPossess`에서 이 델리게이트를 구독해 자신의 Blackboard(`IsDead`)를 갱신하는 방식으로 역할을 분리했다.\n\t`AGoldenGoblinAIController`는 `UAIPerceptionComponent`(Sight, `SightRadius 1500 / LoseSightRadius 1800 / PeripheralVisionAngle 90도`)로 플레이어를 감지해 Blackboard의 `TargetActor`를 갱신하고, 시야를 잃으면 그 값을 지워 BT가 Patrol 브랜치로 복귀하도록 했다. BT 흐름은 평시 배회(Patrol) → 감지 시 회피 기동(Evade) → 사망 시 아이템 드랍 + 소멸(DropItem + Disappear) 세 단계이며, 아래 Task/Decorator로 나눠 구현했다.\n\t<table header-row=\"true\">\n<tr>\n<td>클래스</td>\n<td>역할</td>\n</tr>\n<tr>\n<td>`BTT_GetRandomLocation` / `BTT_PatrolMoveTo`</td>\n<td>배회 지점 선정 및 이동</td>\n</tr>\n<tr>\n<td>`BTT_FindEscapeLocation` / `BTT_EvasiveManeuver`</td>\n<td>도주 지점 계산 및 회피 기동(이동속도를 `EvasiveMoveSpeed`로 전환)</td>\n</tr>\n<tr>\n<td>`BTT_PlayEscapeAction`</td>\n<td>도주 중 회피 몬타주 재생(재생 완료까지 대기하는 레이턴트 태스크)</td>\n</tr>\n<tr>\n<td>`BTD_IsDead`</td>\n<td>Blackboard의 IsDead를 감시해 사망 브랜치 우선순위를 강제하는 Decorator</td>\n</tr>\n<tr>\n<td>`BTT_DropItem` / `BTT_Disappear` / `BTT_StopMovement`</td>\n<td>사망 시 보상 드랍, 소멸 연출, 이동 정지</td>\n</tr>\n\t</table>\n\t데디케이티드 서버는 렌더링을 하지 않고 `SpawnSystemAtLocation`은 리플리케이트되지 않기 때문에, 사망 이펙트는 서버 전용 Task(`BTT_Disappear`)가 `Multicast_PlayDeathEffect` RPC를 호출해 모든 클라이언트가 각자 로컬로 이펙트를 스폰하도록 처리했다.\n\t`BTD_IsDead`를 만들면서 엔진 자체의 함정도 하나 만났다 — 블랙보드 키가 에셋 로드 직후에는 종종 유효하지 않은 ID로 굳어있는 현상이 있어서, 이름(`SelectedKeyName`) 기준으로 강제 리졸브하는 코드를 따로 넣어 우회했다. 죽음 처리 자체도 `TakeDamage` → `HandleDeath` → `OnGoblinDied` 델리게이트 → Blackboard `IsDead` 갱신 → BT 데코레이터 재평가로 이어지는 여러 컴포넌트를 가로지르는 비동기 체인이라, 각 단계마다 로그를 심어 순서대로 제대로 이어지는지 검증하면서 디버깅했다.\n\t### 보상 — 고정값에서 범위 랜덤으로\n\t초기 구현은 고정 골드 보상(`GoldRewardAmount = 5000`)이었다. \"이 몬스터만 잡으면 그 라운드는 확정으로 유리해진다\"는 식으로 공략이 고정돼버리는 문제가 있어서, `MinGoldRewardAmount(100)~MaxGoldRewardAmount(3000)` 범위로 바꾸고 `BeginPlay` 시점에 서버 권위로 한 번만 굴려(`FMath::RandRange`) 고정해두는 방식으로 개선했다.\n\t```c++\nif (HasAuthority())\n{\n    const int32 RangeMin = FMath::Min(MinGoldRewardAmount, MaxGoldRewardAmount);\n    const int32 RangeMax = FMath::Max(MinGoldRewardAmount, MaxGoldRewardAmount);\n    RolledGoldRewardAmount = FMath::RandRange(RangeMin, RangeMax);\n}\n\t```\n\t### HP 위젯\n\t`GoldenGoblinHPWidget`은 `BeginPlay`에서 액터에 붙은 `UWidgetComponent`를 자동으로 찾아 바인딩해서 블루프린트 쪽 별도 배선 없이도 동작하도록 했다. 매 틱 카메라 쪽을 바라보게 회전시키고(빌보드), 카메라와 위젯 사이를 라인 트레이스로 검사해 장애물에 가려지면 자동으로 숨기도록 했다.\n\t## 결과\n\t- 개체 행동(BT)과 등장 페이싱(Director)을 완전히 분리해 설계하니, 스폰 로직만 따로 반복 개선(1회 스폰 → 동시 N마리 풀 유지)할 수 있었음\n\t- 요구사항 정의부터 클래스 구조, BT, 디렉터까지 설계·구현을 완성함\n\t- 보상을 고정값으로 두면 공략이 고정된다는 것을 실제로 체감하고, 범위 랜덤 + 서버 권위 1회 롤링으로 대응\n\t- 이 프로젝트 전체를 관통하는 \"서버 권위\" 원칙(`HasAuthority` 가드)을 신규 시스템에도 처음부터 일관되게 적용\n</details>\n<details>\n<summary>08 - 렌더링 성능 근본 원인 조사</summary>\n\t## 문제/배경\n\tTPS 전투 페이즈에서 프레임이 무겁다는 체감이 있었지만, 정확히 어디가 병목인지는 알