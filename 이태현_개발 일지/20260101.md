# 2026-01-01 (수)

## 오늘 요약 (3줄)
- UE 프로젝트가 경로 문제로 모듈 rebuild 오류가 날 수 있는 상태였음
- `subst X:`로 프로젝트 루트 경로를 고정해서 오류 재발 가능성을 줄임
- VS Code 설치 후 DevLog(인덱스 + 일자 파일) 기록 체계를 만들었음

---

## 1) 배경 / 목표
- 배경: UE 프로젝트 실행 시 모듈 버전/빌드 관련 경고가 뜨는 상황이 있었고, 재부팅 후 경로 문제로 재발할 가능성이 있었음
- 목표:
  - (1) 프로젝트 경로를 고정해서 모듈 rebuild 오류 재발 방지
  - (2) 작업 기록을 남길 DevLog 구조 생성

---

## 2) 문제 상황 정리
### 2-1. 증상(에러 메시지)
- UE 실행 시 아래 메시지가 뜰 수 있음:
  - `The following modules are missing or built with a different engine version: Dopamine_Addiction`
  - `Would you like to rebuild them now?`
- 이후 rebuild 시도 시 실패/오류가 발생할 수 있음(경로/빌드 환경 꼬임 가능)

### 2-2. 원인 추정
- `subst X:`는 재부팅하면 해제됨 → X: 드라이브가 사라짐
- UE 프로젝트를 “X:\...” 기준으로 열어야 안정적인데,
  - 재부팅 후 subst 없이 실행하면 경로가 달라져서(또는 이전 빌드 산출물과 경로가 달라져서)
  - UE가 모듈 빌드/버전 불일치로 판단할 가능성이 있음

---

## 3) 해결 과정
### 3-1. subst로 X: 다시 매핑
- 실행한 명령:
  - `subst X: "C:\Users\mdk73\OneDrive\문서\GitHub\Project-for-Graduation"`

### 3-2. Git LFS 상태 확인(정상 여부 체크)
- `git status` → `working tree clean`
- `git lfs ls-files` → UE 에셋(.uasset 등) LFS 추적 정상 확인
- `git status --ignored` → Binaries/Intermediate/Saved 등은 ignore 정상

---

## 4) 결과 / 검증
- Git 상태:
  - `On branch main`
  - `Your branch is up to date with 'origin/main'`
  - `nothing to commit, working tree clean`
- 결론:
  - 현재 리포지토리 상태는 깨끗하고 LFS도 정상
  - 이후 UE 실행은 **X:\ 기준**으로 통일하는 게 안전

---

## 5) DevLog 기록 체계 구축
### 5-1. 위치
- `Project-for-Graduation/이태현_개발 일지/`

### 5-2. 파일 구조
- `DevLog.md` : 인덱스(월별 + 한줄 요약 + 링크)
- `20260101.md` : 당일 상세 기록

---

## 6) 결정사항(나만의 규칙)
- UE 프로젝트 실행은 항상 `X:\` 경로 기준으로 한다(경로 꼬임 방지)
- DevLog는 “인덱스는 초축약 / 일자 파일에 상세” 원칙으로 작성한다